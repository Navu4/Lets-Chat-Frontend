import React from "react";

import { Flex } from "@chakra-ui/react";
interface Props {}

const ChatBox = (props: Props) => {
  return (
    <Flex flex={1} border={"1px solid black"} h="full">
      ChatBox
    </Flex>
  );
};

export default ChatBox;
